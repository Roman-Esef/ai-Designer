# test — сборка экранов агентом

Сюда агент `ai-designer` кладёт собранные экраны:

- `<Имя>.html` — макет на компонентах дизайн-системы IBP;
- `<Имя>.screen.md` — структура экрана для агента-разработчика.

Экраны подключают `../../DS-IBP/ds.css` и `../../DS-IBP/scripts/ds.js`.
