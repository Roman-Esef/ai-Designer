# Системные файлы macOS: Finder сам создаёт .DS_Store в каждой открытой папке
# (вид папки, расположение значков). Проекту и репозиторию они не нужны.
.DS_Store

# Входящие материалы: скриншоты, палитры, макеты, ТЗ, выгрузки из текущей
# системы. Лежат только локально и в GitHub не уходят; из истории коммитов
# вычищены (22.09.2026). Ссылки на них в спеках и доках — указание источника.
/Uploads/
/DS-IBP/uploads/
